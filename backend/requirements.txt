# Core PDF processing
pymupdf>=1.23.0
Pillow>=10.0.0

# Layout analysis & OCR
# surya-ocr: install separately if needed (requires specific torch version)
# pip install surya-ocr
pytesseract>=0.3.10

# Table structure recognition (optional - heavy dependencies)
# Uncomment if you need table recognition:
# table-transformer>=0.1.0
torch>=2.0.0
torchvision>=0.15.0

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0

# API server
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
websockets>=11.0

# LLM integration (optional)
google-generativeai>=0.3.0
httpx>=0.24.0

# Korean NLP (optional - requires Java)
# konlpy>=0.6.0

# Utilities
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
aiofiles>=23.0.0
